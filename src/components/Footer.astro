---
const year = new Date().getFullYear();

const quickLinks = [
  { href: '/',         label: 'Home' },
  { href: '/about',    label: 'About Us' },
  { href: '/services', label: 'Services' },
  { href: '/projects', label: 'Projects' },
  { href: '/contact',  label: 'Contact' },
];

const services = [
  'General Construction',
  'Renovations & Refurbishments',
  'Interior Finishing',
  'Roofing & Waterproofing',
  'Electrical & Plumbing',
  'Project Management',
];
---

<footer class="footer" aria-label="Site footer">
  <div class="footer__main">
    <div class="container footer__grid">

      <!-- Brand -->
      <div class="footer__brand">
        <a href="/" class="footer__logo" aria-label="ArchiForce home">
          <span class="footer__logo-badge">AF</span>
          <span class="footer__logo-name">ArchiForce</span>
        </a>
        <p class="footer__tagline">
          Building with integrity since 1998. Warsaw's most trusted construction partner for residential, commercial, and industrial projects.
        </p>
        <div class="footer__cert">
          <span class="footer__cert-badge">ISO 9001</span>
          <span class="footer__cert-badge">Licensed &amp; Insured</span>
        </div>
      </div>

      <!-- Quick links -->
      <div class="footer__col">
        <h3 class="footer__heading">Quick Links</h3>
        <ul class="footer__list" role="list">
          {quickLinks.map(link => (
            <li><a href={link.href} class="footer__link">{link.label}</a></li>
          ))}
        </ul>
      </div>

      <!-- Services -->
      <div class="footer__col">
        <h3 class="footer__heading">Our Services</h3>
        <ul class="footer__list" role="list">
          {services.map(s => (
            <li><a href="/services" class="footer__link">{s}</a></li>
          ))}
        </ul>
      </div>

      <!-- Contact -->
      <div class="footer__col">
        <h3 class="footer__heading">Get in Touch</h3>
        <ul class="footer__contact-list" role="list">
          <li>
            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"/><circle cx="12" cy="10" r="3"/></svg>
            <span>48 Industrial Avenue,<br>Warsaw, 00-001, Poland</span>
          </li>
          <li>
            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07A19.5 19.5 0 0 1 4.69 12a19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 3.6 1.28h3a2 2 0 0 1 2 1.72c.127.96.361 1.903.7 2.81a2 2 0 0 1-.45 2.11L7.91 8.37a16 16 0 0 0 6.34 6.34l.96-.96a2 2 0 0 1 2.11-.45c.907.339 1.85.573 2.81.7A2 2 0 0 1 22 16.92z"/></svg>
            <a href="tel:+48221234567">+48 22 123 45 67</a>
          </li>
          <li>
            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/><polyline points="22,6 12,13 2,6"/></svg>
            <a href="mailto:info@archiforce.pl">info@archiforce.pl</a>
          </li>
          <li>
            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>
            <span>Mon–Fri: 7:00 AM – 6:00 PM</span>
          </li>
        </ul>
      </div>

    </div>
  </div>

  <div class="footer__bottom">
    <div class="container footer__bottom-inner">
      <p>&copy; {year} ArchiForce Construction Sp. z o.o. All rights reserved.</p>
      <p>Designed &amp; built with <a href="https://astro.build" target="_blank" rel="noopener" class="footer__astro-link">Astro</a></p>
    </div>
  </div>
</footer>

<style lang="scss">
  @use '../styles/variables' as *;

  .footer {
    background-color: $color-dark;
    color: rgba($color-white, 0.65);

    &__main {
      padding-block: $spacing-2xl $spacing-xl;
    }

    &__grid {
      display: grid;
      grid-template-columns: 1.8fr 1fr 1fr 1.4fr;
      gap: $spacing-xl;

      @media (max-width: #{$bp-lg}) {
        grid-template-columns: 1fr 1fr;
        gap: $spacing-lg;
      }

      @media (max-width: #{$bp-sm}) {
        grid-template-columns: 1fr;
        gap: $spacing-lg;
      }
    }

    // ── Brand ───────────────────────────────────────────────────────────────
    &__logo {
      display: inline-flex;
      align-items: center;
      gap: 0.6rem;
      margin-bottom: $spacing-sm;
      transition: opacity $transition-base;

      &:hover { opacity: 0.8; }
    }

    &__logo-badge {
      display: flex;
      align-items: center;
      justify-content: center;
      width: 36px;
      height: 36px;
      background-color: $color-accent;
      color: $color-dark;
      font-weight: 800;
      font-size: 0.75rem;
      border-radius: $radius-sm;
      flex-shrink: 0;
    }

    &__logo-name {
      font-size: $font-size-lg;
      font-weight: 700;
      color: $color-white;
    }

    &__tagline {
      font-size: $font-size-sm;
      line-height: 1.75;
      max-width: 300px;
      margin-bottom: $spacing-md;
    }

    &__cert {
      display: flex;
      flex-wrap: wrap;
      gap: $spacing-xs;
    }

    &__cert-badge {
      font-size: $font-size-xs;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      padding: 0.25rem 0.6rem;
      border: 1px solid rgba($color-accent, 0.5);
      border-radius: $radius-sm;
      color: $color-accent;
    }

    // ── Columns ─────────────────────────────────────────────────────────────
    &__heading {
      color: $color-white;
      font-size: $font-size-sm;
      text-transform: uppercase;
      letter-spacing: 1.5px;
      margin-bottom: $spacing-md;
      padding-bottom: $spacing-xs;
      border-bottom: 2px solid $color-accent;
      display: inline-block;
    }

    &__list {
      display: flex;
      flex-direction: column;
      gap: 0.55rem;
    }

    &__link {
      font-size: $font-size-sm;
      transition: color $transition-base;

      &:hover,
      &:focus-visible {
        color: $color-accent;
        outline: none;
      }
    }

    // ── Contact ─────────────────────────────────────────────────────────────
    &__contact-list {
      display: flex;
      flex-direction: column;
      gap: 0.85rem;

      li {
        display: flex;
        align-items: flex-start;
        gap: 0.6rem;
        font-size: $font-size-sm;
        line-height: 1.5;

        svg {
          flex-shrink: 0;
          margin-top: 2px;
          color: $color-accent;
        }

        a {
          transition: color $transition-base;
          &:hover { color: $color-accent; }
        }
      }
    }

    // ── Bottom bar ──────────────────────────────────────────────────────────
    &__bottom {
      padding-block: $spacing-sm;
      border-top: 1px solid rgba($color-white, 0.08);
    }

    &__bottom-inner {
      display: flex;
      justify-content: space-between;
      align-items: center;
      flex-wrap: wrap;
      gap: $spacing-sm;
      font-size: $font-size-xs;

      @media (max-width: #{$bp-sm}) {
        flex-direction: column;
        text-align: center;
      }
    }

    &__astro-link {
      color: $color-accent;
      &:hover { text-decoration: underline; }
    }
  }
</style>
